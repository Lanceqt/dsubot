"""Discord command modules for the DSU bot.

This package contains all command sets and handlers that define the bot's
interaction capabilities. Each module within this package represents a logical
grouping of related commands that users can invoke through Discord's interface.

Commands are implemented using disnake's command framework and follow the
slash command pattern for better user experience and discoverability.

Modules:
    [List of command modules will appear here as they are added]

Examples:
    Commands in this package are automatically registered with the bot
    when it starts up and can be invoked by users with appropriate permissions.

"""
